<ul>
<% @songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %> 
    <% link_parts = display_artist(song) %>
    <a href="<%= link_parts[0] %>"> <%= link_parts[1] %></a>
  </li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
